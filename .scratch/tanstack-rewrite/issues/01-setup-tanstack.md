Status: ready-for-agent

# 01 - Setup TanStack Start & Foundation

## Objective
Initialize the new project structure with the required tooling.

## Tasks
1. Scaffold a new TanStack Start project with React.
2. Install Tailwind CSS and configure `tailwind.config.js`.
3. Initialize `shadcn/ui` and install base primitives: `Button`, `Card`, `Badge`.
4. Configure standard CSS variables (colors/radius) to match our existing `index.html` design system.
5. Ensure the dev server runs and the root route loads correctly.
