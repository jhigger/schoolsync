Status: done
Blockers: 02-app-shell.md

# 03 - Dashboard View

## Objective
Migrate the Dashboard screen UI into the new `/dashboard` route.

## Tasks
1. Create the `StatusAlert` banner component.
2. Create the `SummaryStrip` component.
3. Migrate the "What to do now" (Hero Task list).
4. Ensure mobile responsive stacking rules (flex-col on small screens) are applied via Tailwind classes.
