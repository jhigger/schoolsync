# 11 — Tab Body Padding Consistency

**What to build:** Standardize the padding and layout spacing across all tab bodies. Currently, padding is inconsistent (some centered, some full-width, some using larger padding).
**Status:** ready-for-agent

- [ ] Review the original HTML's standard padding/spacing for `.view` or tab bodies.
- [ ] Apply a consistent layout wrapper or consistent classes (e.g., `p-[18px_22px] max-w-full` etc.) across all route components to unify the UX.
